const SectionTitle = ({ title }: { title: string }) => {
  return (
    <div>
      <h2 className="text-2xl text-primary-text font-semibold">{title}</h2>
    </div>
  );
};

export default SectionTitle;
